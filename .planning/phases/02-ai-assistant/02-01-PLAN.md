---
phase: 02-ai-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/.env
  - backend/.env.example
autonomous: false
requirements: [AI-01, AI-02]
user_setup:
  - service: gemini
    why: "Triage LLM reasoning"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio"
      - name: GEMINI_MODEL
        source: "Set to gemini-2.0-flash by default"
  - service: supabase
    why: "Database and real-time"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Project Settings -> API"
      - name: SUPABASE_SERVICE_KEY
        source: "Supabase Project Settings -> API"
must_haves:
  truths:
    - "User can provide Gemini API key and necessary env vars in backend/.env"
    - "The system successfully starts the backend and frontend with the credentials."
    - "The user can interact with the AI assistant through the kiosk frontend and describe problems."
  artifacts:
    - path: "backend/.env"
      provides: "Environment variables for Gemini, Redis, and Supabase"
    - path: "backend/services/claude_service.py"
      provides: "Agentic triage LLM execution"
  key_links:
    - from: "backend/services/claude_service.py"
      to: "Gemini API"
      via: "HTTpx calls with GEMINI_API_KEY"
---

<objective>
Set up the AI assistant with proper API keys and ensure the end-to-end triage flow works.

Purpose: To allow users to describe their own set of problems, testing the full LLM setup and confirming the working state of the kiosk.
Output: Configured `.env` file, verified running services, and confirmed working AI triage chat.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@backend/.env.example
@backend/services/claude_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Environment File</name>
  <files>backend/.env</files>
  <action>Check if `backend/.env` exists. If not, copy the contents of `backend/.env.example` into `backend/.env`. Ensure the keys `GEMINI_API_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, and `REDIS_URL` are present.</action>
  <verify>
    <automated>test -f backend/.env && grep GEMINI_API_KEY backend/.env</automated>
  </verify>
  <done>`backend/.env` file is prepared for user configuration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Created the .env file scaffold for configuration</what-built>
  <how-to-verify>
    1. Open `backend/.env`.
    2. Provide your actual `GEMINI_API_KEY`.
    3. Provide your `SUPABASE_URL` and `SUPABASE_SERVICE_KEY`.
    4. Ensure Redis is running or use a valid `REDIS_URL`.
  </how-to-verify>
  <resume-signal>Type "approved" once you have filled in the `.env` values.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify End-to-End AI Flow</name>
  <files>backend/main.py</files>
  <action>Start Redis (if local), the backend FastAPI server, and the frontend Next.js server in the background. Note: Use `uvicorn main:app --reload` in `backend` and `npm run dev` in `frontend`.</action>
  <verify>
    <automated>curl -s -f http://127.0.0.1:8000/docs > /dev/null</automated>
  </verify>
  <done>Servers started and responding to HTTP requests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Started backend and frontend services</what-built>
  <how-to-verify>
    1. Open the Pyrexia kiosk UI at http://localhost:3000/kiosk
    2. Attempt to describe a set of problems (e.g., "I have a severe headache and fever").
    3. Confirm that Pyrexia responds interactively as a clinical triage assistant.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any errors observed in the flow.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User inputs free-text medical queries |
| API → Gemini | Sends transcribed text / PII to LLM provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | LLM Config | accept | Requires API Key in `.env` |
| T-02-02 | Info Disclosure | API → Gemini | accept | Patient descriptions sent to Gemini as part of core flow |
</threat_model>

<verification>
- Backend `.env` correctly configured with the Gemini API key.
- Triage endpoint correctly queries Gemini and streams responses to the frontend.
</verification>

<success_criteria>
User successfully queries the AI assistant with custom problems via the UI and receives responses.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-assistant/02-01-SUMMARY.md`
</output>